/** Shown when server catalog queries run without public Supabase env. */
export const DEALS_UNAVAILABLE_WITHOUT_PUBLIC_SUPABASE =
  'Deals are unavailable because Supabase is not configured. Set NEXT_PUBLIC_SUPABASE_URL and NEXT_PUBLIC_SUPABASE_ANON_KEY.';
